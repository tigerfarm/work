import { Configuration } from './configuration';
import { RegistrationState, UpdateReason, Connector } from './connector';
/**
 * Registrar connector implementation for twilsock
 */
declare class TwilsockConnector extends Connector {
    private readonly twilsock;
    private context;
    constructor(context: any, twilsock: any, config: Configuration);
    setNotificationId(...args: any[]): void;
    updateToken(token: string): void;
    private updateContextRequest;
    protected updateRegistration(registration: RegistrationState, reasons: Set<UpdateReason>): Promise<RegistrationState>;
    protected removeRegistration(): Promise<void>;
}
export { TwilsockConnector };
