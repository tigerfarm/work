import { TreeMap } from './utils/tree';
interface CacheEntry<T> {
    value: T;
    revision: number;
    isValid: boolean;
}
declare class Cache<K, V> {
    readonly items: TreeMap<K, CacheEntry<V>>;
    constructor();
    store(key: K, value: V, revision: number): V;
    delete(key: K, revision: number, force?: boolean): void;
    isKnown(key: K, revision: number): boolean;
    get(key: K): V;
    has(key: K): boolean;
    forEach(callbackfn: (key: K, value: V) => void): void;
}
export { Cache };
export default Cache;
