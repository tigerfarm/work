import { Protocol } from '../protocol';
import { AbstractMessage } from './abstractmessage';
declare class TelemetryEvent {
    readonly start: number;
    readonly end: number;
    readonly title: string;
    readonly details: string;
    readonly id?: string;
    readonly type?: string;
    constructor(start: number, // relative to event send time
    end: number, // relative to event send time
    title: string, details: string, id?: string, // optional, default will be random assigned by backend
    type?: string);
}
declare class Telemetry extends AbstractMessage {
    readonly method: Protocol.MessageType;
    events: TelemetryEvent[];
    constructor(events: TelemetryEvent[]);
}
export { Telemetry, TelemetryEvent };
